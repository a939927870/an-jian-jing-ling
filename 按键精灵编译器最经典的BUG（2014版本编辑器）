x = 0
n = 100
num = Array(18, 0, 0, 0, 0, 0, 0, 0, 0, 0)

Lite = Round(（num(0) / n）, 2)
TracePrint Lite


/*
这个代码保存不报错，但是调试输出后显示：
脚本 我的脚本3.Q ，第19行：类型不匹配: 'VEEC22B203E78756D', 
*/
/*
BUG还原方法：
1.编译以上代码，编译器报错。
2.然后去掉“（num(0) / n）”左右的小括号，变成：Lite = Round(num(0) / n, 2)，编译不报错。
3.然后手动键盘输入（请注意，这里必须键盘输入Ctrl + Z还原就没有这个效果了），去掉的小括号，编译就不报错了！
4.把上面这段原始代码直接覆盖上去，请注意，这里现在是完全一模一样的代码了！直接覆盖！编译器报错！
*/
